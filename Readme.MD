## creat a Venv to avoid Package conflits 

## install unstructer from and add to the bin path in enviornment variable (note this has to be system wide)

## install Tessaract-ocr exe and add the directory path to the enviornment variable as well 

## In virtual env install 
## pip install "numpy<2.0.0" "opencv-python<4.9" "unstructured[all-docs]"

## pip install python-magic-bin (for Windows specific)